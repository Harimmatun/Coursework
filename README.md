
# LMS API System (Learning Management System)

## 📖 Опис проєкту
**LMS API System** — це серверна частина (Backend) веб-додатку для управління освітнім процесом. Система дозволяє організувати повний цикл онлайн-навчання: від створення курсів викладачами до здачі домашніх завдань студентами та їх оцінювання.

### 🎯 Обґрунтування вибору предметної області
Предметна область "Система управління навчанням" була обрана для курсової роботи з кількох причин:
1.  **Складність зв'язків:** Вона дозволяє продемонструвати роботу з усіма типами реляційних зв'язків:
    * *Один-до-багатьох* (Викладач -> Курси).
    * *Багато-до-багатьох* (Студенти <-> Курси).
    * *Каскадні видалення* (Курс -> Модулі).
2.  **Бізнес-логіка:** Наявність чітких сценаріїв використання (зарахування, перевірка дедлайнів, валідація оцінок).

---

## 👥 Розробник
* **Студент:** Красуля Дмитро
* **Група:** ІМ-42

---

## 🛠 Технологічний стек


| Категорія | Технологія | Версія | Призначення |
|---|---|---|---|
| **Core** | **Python** | 3.14 | Основна мова програмування |
| **Web Framework** | **FastAPI** | 0.100+ | Високопродуктивний асинхронний фреймворк для створення API |
| **Database** | **PostgreSQL** | 15+ | Основна реляційна база даних |
| **ORM** | **SQLAlchemy** | 2.0 | Об'єктно-реляційне відображення (робота з БД через класи) |
| **Migrations** | **Alembic** | Latest | Система контролю версій схеми бази даних |
| **Validation** | **Pydantic** | V2 | Валідація та серіалізація вхідних даних (Schemas) |
| **Testing** | **Pytest** | Latest | Фреймворк для модульного та інтеграційного тестування |
| **Frontend** | **Jinja2** | Latest | Шаблонізатор для генерації HTML-сторінок |
| **Containerization** | **Docker** | Optional |  Для розгортання середовища |


## ⚙️ Інструкції з налаштування (Windows)

### 📋 Попередні вимоги
Переконайтеся, що у вас встановлено:
* Python 3.10 або новіше
* PostgreSQL (і створена пуста база даних `lms_db`)
* Git

### Крок 1. Клонування та підготовка
Відкрийте термінал (PowerShell) у папці, де хочете зберегти проєкт:

```bash
git clone https://github.com/Harimmatun/Coursework.git
cd CourseWork
```

### Крок 2. Віртуальне середовище
Створіть та активуйте ізольоване середовище для бібліотек:

```powershell
python -m venv venv
.\venv\Scripts\activate
```

### Крок 3. Встановлення залежностей
```powershell
pip install -r requirements.txt
```

### Крок 4. Налаштування бази даних
1. Створіть файл `.env` у корені проєкту.
2. Вставте туди рядок підключення (замініть `YOUR_PASSWORD` на ваш пароль):

```ini
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/lms_db
```

### Крок 5. Створення таблиць
Запустіть міграції, щоб створити структуру бази даних:

```powershell
alembic upgrade head
```

*(Опціонально) Заповнити базу тестовими даними:*
```powershell
cd src
python main.py
cd ..
```

---

## ▶️ Запуск та Використання

### Запуск сервера
Виконуйте цю команду з кореневої папки проєкту:

```powershell
uvicorn src.main_api:app --reload
```

Після запуску відкрийте у браузері:
* **Головна сторінка:** http://127.0.0.1:8000/
* **Документація API (Swagger):** http://127.0.0.1:8000/docs

---

## 🧪 Тестування

Для перевірки стабільності системи запустіть тести:

```powershell
# Всі тести разом
pytest -v

# Тільки API тести
pytest tests/test_api.py -v
```

---

## 📂 Структура проєкту

```text
CourseWork/
├── src/
│   ├── models/          # Таблиці бази даних (SQLAlchemy Models)
│   ├── repositories/    # CRUD операції (робота з БД)
│   ├── templates/       # HTML файли сайту (Jinja2)
│   ├── main.py          # Скрипт ініціалізації та наповнення даними (Seed)
│   ├── main_api.py      # Головний файл сервера (Endpoints)
│   ├── schemas.py       # Pydantic схеми (Валідація даних)
│   ├── services.py      # Бізнес-логіка додатку
│   ├── utils.py         # Допоміжні функції (напр. розрахунок оцінок)
│   └── database.py      # Підключення до БД
├── tests/               # Папка з тестами (Unit, Integration, API)
├── migrations/          # Файли міграцій Alembic
├── .env                 # Налаштування (не завантажувати на GitHub!)
├── requirements.txt     # Список бібліотек
├── Dockerfile           # Інструкція для збірки Docker образу
├── docker-compose.yml   # Налаштування запуску контейнерів
└── README.md            # Цей файл
```

